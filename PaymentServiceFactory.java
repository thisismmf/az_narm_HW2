/**
 * This factory is no longer needed.
 * Logic moved to PaymentType enum (createService).
 * File kept for legacy purposes or possible extension.
 */
public class PaymentServiceFactory {
    // Deprecated: use PaymentType.createService(config) instead.
}
